# 256色対応のターミナル定義を使用
term screen-256color

# 背景色消去(BCE)の設定（Vim等の背景色表示に影響）
defbce on

truecolor on

termcapinfo xterm*|vte*|gnome*|alacritty* Tc:Cc=\E[%?%p1%{2}%<%t4%p1%d%e48;2;%p2%d;%p3%d;%p4%d%;m

# ウインドウ1から始めることで、切り替えを押しやすく
bind c screen 1
bind ^c screen 1
bind 0 select 10                                                            
screen 1

# ターミナルのスクロールバーを有効にする（Alternate Screenを無効化）
termcapinfo xterm* ti@:te@
# スクロールバックの行数を増やす
defscrollback 10000

caption always "%{= .b}%{W} %-w%{=u Yk} [%n %t] %{-}%{W}%+w %=%{c} [%H] %{W}%Y-%m-%d %c "
